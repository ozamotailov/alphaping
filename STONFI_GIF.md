# GIF-хук «alert → Buy on STON.fi → swap» (под питч STON.fi)

**Назначение:** короткая зацикленная GIF-ка для поста в @stonfidex и для DM-питча (см. STONFI_FEATURE.md).
Это НЕ демо-видео (то — DEMO.md, 75с со звуком/субтитрами). GIF — **хук на 8–12 сек, без звука,
лупится**, показывает ровно одну вещь: сигнал превращается в своп на STON.fi в один тап.

## Спеки
- **Длина: 8–12 сек.** Короче лучше — GIF должен «прочитываться» за один цикл.
- **Без звука, бесшовный луп.** Начни и закончи на одном кадре (логотип TonSonar или карточка алерта) — петля не «дёргается».
- **Вертикаль 9:16** (запись телефона) — нативно для Telegram-ленты. Десктоп 16:9 тоже ок, но вертикаль сильнее.
- **Минимум текста.** GIF читают без подписей; 1–2 коротких оверлея-лейбла максимум (≤ 3 слова), крупно. Без полноценных субтитров.
- **Крупный UI.** Кнопка «Buy on STON.fi» и экран котировки — главные объекты, должны занимать центр кадра.
- **Темп быстрый.** Любые загрузки/ожидания ускорить 3–5× или вырезать.
- **Размер файла:** держать ≤ ~10 МБ (Telegram сам сконвертит GIF→mp4, но лёгкий файл грузится у всех).

## Раскадровка (≈10 сек, 4 бита)

| Бит | Время | Что на экране | Оверлей (опц., ≤3 слова) |
|---|---|---|---|
| 1 | 0–2.5с | Карточка **алерта** в боте: «🟢 Smart money bought TOKEN» с кнопкой **«Buy on STON.fi»**. Палец/курсор подлетает к кнопке. | *Smart money moves* |
| 2 | 2.5–4с | **Тап** → открывается Mini App, своп-панель уже на этом токене (pre-filled). | *One tap* |
| 3 | 4–7.5с | Своп-панель: сумма TON → **живая котировка STON.fi** «≈ X TOKEN · impact …» → тап **«Swap via TON Connect»**. | *STON.fi swap* |
| 4 | 7.5–10с | Кошелёк показывает транзакцию → ✅ успех → быстрый возврат к логотипу TonSonar (точка склейки лупа). | *Done — in Telegram* |

**Money shot = биты 1→3** (алерт → кнопка → котировка STON.fi). Если ужимаешь до 8с — режь бит 4
до короткого ✅ и склейку на логотип. Биты 1 и 3 не торопить — на них держится смысл.

## Как записать
1. **Подготовь алерт:** `/demo` (admin) пришлёт геройский алерт «smart-money bought» с кнопкой свопа,
   форсированно EN (чисто для записи). Либо `/testlisting` — листинг-алерт с кнопкой.
2. **Подготовь данные:** кошелёк с 1–2 TON подключён; токен в алерте ликвидный (котировка считается живо).
3. **Запиши экран** (телефон: встроенная запись; или Telegram Desktop + OBS).
4. Можно сделать **реальный мелкий своп** (0.3–0.5 TON) ради живого кадра подписи — но для GIF
   достаточно дойти до экрана подтверждения, полная подпись не обязательна (это хук, не пруф).

## Как сделать GIF из записи (ffmpeg, бесплатно)
Двухпроходный рендер с палитрой = резкий GIF без грязи. `IN.mp4` — твоя запись, ускорь паузы заранее в CapCut или флагом `setpts`.

```bash
# 1) палитра (точные цвета UI/градиента)
ffmpeg -i IN.mp4 -vf "fps=15,scale=540:-1:flags=lanczos,palettegen=stats_mode=diff" -y palette.png

# 2) собрать GIF по палитре (fps 15 + ширина 540 = баланс плавность/вес)
ffmpeg -i IN.mp4 -i palette.png -lavfi "fps=15,scale=540:-1:flags=lanczos[x];[x][1:v]paletteuse=dither=bayer:bayer_scale=3" -y tonsonar.gif

# обрезать по времени (пример: взять с 00:02 длиной 10с) — добавь к шагам -ss 2 -t 10
# ускорить исходник 1.5× перед нарезкой: -vf "setpts=PTS/1.5,fps=15,scale=540:-1..."
```

Альтернатива выше качеством/меньше вес — **gifski** (`ffmpeg ... -f image2pipe` → `gifski`), но
двухпроходный ffmpeg обычно достаточно. Если вес > 10 МБ: понизь до `fps=12` и/или `scale=480`,
укороти до 8с.

## Чек-лист «GIF готов»
- [ ] Видна **карточка алерта с кнопкой** «Buy on STON.fi».
- [ ] Виден **тап → своп-панель** (переход по deep-link).
- [ ] Видна **живая котировка STON.fi** (сумма + impact) — это и есть «их» кадр.
- [ ] Луп **бесшовный** (старт=финиш), длина 8–12с, без звука.
- [ ] Файл ≤ ~10 МБ, вертикаль 9:16, UI крупный и читаемый.
- [ ] EN-интерфейс (через `/demo`), без RU-текста в кадре.
